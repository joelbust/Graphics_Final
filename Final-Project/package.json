{
  "name": "lower-case-dashed-name-of-project",
  "repository": "Graphics_Final",
  "version": "0.1.0",
  "title": "Graphics Final",
  "description": "SHORT DESCRIPTION OF PROJECT HERE. ONE TO TWO SENTENCES MAX",
  "keywords": [
    "FSU",
    "graphics",
    "three.js",
    "CAP4730 / CAP5726",
    "ivy league",
    "keyword1",
    "keyword2",
    "...",
    "this last entry CANNOT have a comma after it ->"
  ],
  "scripts": {
    "start": "NODE_OPTIONS=--openssl-legacy-provider webpack-dev-server --mode development --output-public-path \"\"",
    "prebuild": "rimraf ./build && mkdirp ./build",
    "build": "NODE_OPTIONS=--openssl-legacy-provider npm run prebuild && NODE_OPTIONS=--openssl-legacy-provider webpack --mode production --progress --output-public-path \"\"",
    "format": "prettier --write '*.js' 'src/**/*.js'",
    "deploy": "NODE_OPTIONS=--openssl-legacy-provider npm run prebuild && NODE_OPTIONS=--openssl-legacy-provider webpack --mode production --progress && gh-pages -d build"
  },
  "dependencies": {
    "dat.gui": "^0.7.7",
    "three": "^0.116.0",
    "three-obj-loader": "^1.1.3",
    "webpack-cli": "^3.3.11"
  },
  "devDependencies": {
    "@babel/core": "^7.9.6",
    "@babel/preset-env": "^7.9.6",
    "babel-loader": "^8.1.0",
    "file-loader": "^6.0.0",
    "gh-pages": "^2.2.0",
    "html-webpack-plugin": "^4.3.0",
    "mkdirp": "^1.0.4",
    "prettier": "^2.0.5",
    "raw-loader": "^4.0.1",
    "rimraf": "^3.0.2",
    "webpack": "^4.43.0",
    "webpack-dev-server": "^3.10.3"
  },
  "engines": {
    "node": ">=8.0.0"
  }
}
